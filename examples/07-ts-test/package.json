{
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dester": "dester ./src ./dist",
    "build": "npm run -s dester",
    "dev": "npm run -s dester -- -w"
  },
  "devDependencies": {
    "dester": "0.x"
  }
}
