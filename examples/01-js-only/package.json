{
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dester": "dester ./src ./dist --no-tsc --no-t",
    "build": "rm -rf ./dist && npm run -s dester",
    "dev": "npm run -s dester -- -w"
  },
  "devDependencies": {
    "dester": "0.x"
  }
}
