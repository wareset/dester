{
  "files": [
    "README.md"
  ],
  "scripts": {
    "dester": "dester ./src ./dist --tsc ./tsconfig.json",
    "build": "rm -rf ./dist && npm run -s dester",
    "dev": "npm run -s dester -- -w"
  },
  "devDependencies": {
    "typescript": "4.x",
    "tslib": "^2.2.0",
    "dester": "0.x"
  }
}
